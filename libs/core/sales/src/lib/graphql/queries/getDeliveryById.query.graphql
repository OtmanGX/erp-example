query GetDeliveryById($id: uuid!) {
  sales_delivery_by_pk(id: $id) {
    id
    status
    client
    company
    contact
    payment_method
    predicted_date
    orders{
      order_id
    }
    delivery_lines {
      id
      amount
      company_name
      delivered
      isReturned
      product
      quantity
      product_label
    }
  }
}
