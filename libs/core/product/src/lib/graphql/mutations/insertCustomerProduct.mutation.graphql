mutation InsertCustomerProduct($code: String, $label: String, $thickness: Int) {
  insert_product_customer_product_one(object: {code: $code, label: $label, thickness: $thickness}) {
    code
    label
    thickness
  }
}
