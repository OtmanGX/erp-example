mutation InsertCustomerProduct($productcode: String, $thickness: numeric) {
  insert_product_customer_product_one(object: {productcode: $productcode, thickness: $thickness}) {
    productcode
    thickness
  }
}
