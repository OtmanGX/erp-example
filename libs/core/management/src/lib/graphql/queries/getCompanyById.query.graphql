query GetCompanyById($_id: uuid! ) {
  management_Company(where: {id: {_eq: $_id}}) {
    CNSS
    ICE
    IF
    RC
    address
    email
    name
    phone
    website
    createdAt
    createdBy
    updatedAt
    updatedBy
    id
  }
}

