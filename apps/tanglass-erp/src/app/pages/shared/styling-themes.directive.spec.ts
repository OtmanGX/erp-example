import { StylingThemesDirective } from './styling-themes.directive';

describe('StylingThemesDirective', () => {
  it('should create an instance', () => {
    const directive = new StylingThemesDirective();
    expect(directive).toBeTruthy();
  });
});
